# Copy to .env and adjust as needed.

# Postgres credentials (used by the db, api services)
POSTGRES_USER=debt
POSTGRES_PASSWORD=debt
POSTGRES_DB=debt_tracker

# CORS — "*" for local use, or a comma-separated list of origins
CORS_ORIGINS=*
